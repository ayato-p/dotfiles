# chezmoi の配布対象から除外する（パターンは ~ からの相対パス）

# Ansible playbook（ansible-pull 用。chezmoi が ~/local.yml として撒くのを防ぐ）
local.yml

# 将来 ansible/ 配下に role 等を増やしたとき用
ansible

# リポジトリの README を ~/README.md として配らない
README.md
