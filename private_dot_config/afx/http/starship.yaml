http:
  - name: starship
    description: The cross-shell prompt for astronauts
    url: https://starship.rs/install.sh
    command:
      build:
        steps:
          - sh install.sh -y --bin-dir ./
      link:
        - from: starship
      snippet: |
        eval "$(starship init bash)"